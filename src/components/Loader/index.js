import React from 'react';

import './style.css';

const Loader = () => (
  <div className="loading-container">
    <div className="loader" />
    <div className="loader-text">No Posts! Visit Later...</div>
  </div>
);

export default Loader;
